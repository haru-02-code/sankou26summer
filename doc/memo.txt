MySQLに直接ログインを試す（起動確認と接続確認を同時に行う）
windows powershellで
mysql -u root -p

実現したいこと
・評価を１～１０ではなくもっと他にいい方法がないか
・殿堂入りリストの作成
・ウォッチリストとどのサブスクサービスで見ればいいか
・バリデーションエラーをフォーム画面に戻って、赤字でエラーメッセージを表示する
・画像選択時にプレビュー表示（jQueryで実装予定）
・同じ画像を使って複数登録したときに削除したときに画像削除で同じ画像を使ったとこからも画像を削除してしまう(同じファイル名だから)
・アカウント登録機能作って他の人のも見れるようにしたい

進め方
① UPDATE/DELETE → 今のINSERT/SELECTの延長で作りやすく、画面（一覧画面に編集・削除ボタン）も必要になるので早めに着手すると全体像が見えやすい
② 正規表現バリデーション → INSERT処理に追記する形なので、今のうちに固めておくとUPDATE処理にも使い回せる
③ 画像アップロード＋物理削除 → INSERT/UPDATE/DELETEの仕組みができてから追加すると、既存のCRUDにスムーズに組み込める
ここで一度デザインに着手する（現状ある機能のみのデザインを考え、将来的に追加したいことのデザインはまだ考えない）
④ Gmail通知 → 独立した機能なので、CRUDが固まってから追加でOK
⑤ オブジェクト指向設計へのリファクタリング → 全機能が動いた後、コードを整理する形で最後に行うのが自然（先に一部だけクラス化すると、後の機能追加のたびに書き方がブレる可能性があるため）


今日学んだこと

8/20

8/28
ここからの進め方
ステップ1: 詳細ページ用の新ルートを作る（/movie/<id>）

まず「1件のデータを取得して表示する」という新しい機能を先に完成させます。edit_formと似た構造（idを受け取ってSELECT）なので、応用しやすいはずです。

ステップ2: 詳細ページのテンプレート（movie_detail.html）を実データで作る

プレビューではダミーデータでしたが、ここで実際にmovie[0]〜movie[6]を差し込みます。

ステップ3: index.htmlをポスターグリッドに変更し、詳細ページへのリンクを繋ぐ

一覧画面の見た目を変更し、各ポスターのクリック先をステップ1で作ったルートに向けます。

ステップ4: 並び替え機能を実装する

「登録日順／名前順／スコア順」の切り替え（ORDER BYを動的に変える）を実装します。

ステップ5: add.html・edit.htmlのスタイリング

フォーム画面も同じダークテーマに合わせます。

ステップ6: 画像プレビュー（jQuery、以前保留にしていたもの）


インデックス	カラム
movie[0]	id
movie[1]	title
movie[2]	genre
movie[3]	watched_date
movie[4]	rating
movie[5]	review
movie[6]	image_path
movie[7]	backdrop_path

index.htmlやり残し
・アプリタイトルをもっとそれらしくする
・jQueryで画像の縦横を判定して縦長画像の上下の隙間を無くす
・並べ替えプルダウン・新規登録ボタンをミニマルさはありつつもっと洗練された雰囲気に
・ポスター下の作品名などの中央表示を試す
・ポスター下の文字表示を削除する機能(ポスターだけ見て何の作品かわかる人用・鑑賞用)
・背景の色をletterboxdに寄せて(どっちでもいい)、ポスターに影を付けてが浮き出ているように見せる

movie_detail.html 


8/31　
一度TMDB連携を行う処理に脱線する
続きはindex.htmlのトグルボタンのアニメーションから

TMDbのキー	　意味	自分のDBでの使い道
title	    　タイトル	　　　　　　　　　　titleカラムにそのまま使える
overview	　あらすじ	　　　　　　　　　　review（感想欄）の初期値、または別枠で表示
poster_path	　ポスター画像の部分的なパス	画像の完全なURLを組み立てる材料
release_date　公開日	　　　　　　　　　　参考情報として表示（DBに保存するかは要検討）




今回のコードは最終的にリファクタリングしてから最後は絶対に理解できるようになってから提出すること(すべての部分において)

レビューのスコア付けはもっと視覚的にリッチなものにする(メーター上げるとか)
log一覧の並び替えに視聴日順を追加
現状の登録順は登録順に並んでいるが表示されている日付は視聴日になっている

作品詳細画面の作品タイトルは作品名が長い場合は文字間隔を狭くする判定を作る
タイトルが長いとポスター画像(その他すべての要素)が左に押されるのも気になる

検索画面の検索バー周りのデザインが気になる

マイログ画面にマイログというタイトルの追加
書いたログは作品詳細画面下に表示されるようにしたい(削除もここから)

ワンピースの映画がONE PIECEでしかヒットしない問題
感想を書くときに超長文になると見えなくなってしまう

＋ログボタンからモーダルでログ追加のデザインはあとで詳細詰めていく(ここはletterboxdの画面claudeに見せてそれよりもいいUIを作りたい)
そのほかの画面に対してもUIは現状letterboxdのまんまだから変えていかないといけないと思う
自分で思いつかなければ他の映画まとめサイトとのキメラにするとか

余裕あったら
保存後のトースト通知（「レビューが追加されました」のような、一時的な通知）→ まだ未実装

cloudflareにデプロイしたい

マイログ画面・検索画面の作品詳細画面からウォッチリスト追加したときの画面遷移はいらない
そのままの画面でいい

検索のサジェストを人気順にしたい

映画の長さの情報も欲しい(ほかにtmdbに登録されている情報で使えるのないか調べる)
映画詳細画面の公開日はフォーマット変えたい

作品検索欄の右端に✕ボタン置いて今入力中の内容全リセット
検索履歴にもｘで削除できるようにする

アプリロゴの作成それに伴ってアプリ内のアクセントcolorを一新


優先順位まとめ
フェーズ1：バグ・データ不整合
 ・マイログ並び替えに「視聴日順」を追加（登録日順とは別の選択肢として）
 ・「ワンピース」で「ONE PIECE」がヒットしない問題（TMDb側の限界、一旦保留してフェーズ3で再考）
 ・長文感想がはみ出す問題（発生箇所を要確認）
フェーズ2：欠けている核となる機能
 ・作品詳細画面に、自分が書いたレビューを表示（複数回視聴分すべてリスト表示）・削除できるようにする
 ・マイログ画面に「マイログ」というタイトルを追加
 ・検索欄にクリア×ボタン、検索履歴の個別削除×ボタンを追加
 ・上映時間など、TMDbの他の使える情報を調査(アニメならエピソード数とか)
 ・詳細画面の公開日フォーマットを変更
フェーズ3：デザイン刷新（まとめて実施）
 ・スコアのビジュアル強化（メーターなど）
 ・長いタイトルの文字間隔・レイアウト崩れ対応
 ・検索バー周りのデザイン
 ・「+ログ」モーダルのUI詰め（Letterboxdと比較しつつ、独自のより良いUIを検討）
 ・全画面のLetterboxd感を払拭し、独自デザインへ刷新（他の映画まとめサイトとのキメラも検討）
 ・ロゴ作成＋アクセントカラー刷新
 ・「ワンピース」検索問題の再検討
フェーズ4：余裕があれば
 ・保存後のトースト通知
 ・検索サジェストを人気順に
フェーズ5：インフラ・最終工程
 ・Cloudflareへのデプロイ
 ・最終リファクタリング＋全コードの完全理解（提出直前、まとまった時間を確保）


追加
 ウォッチリスト、マイログ一覧にすべて、映画、TV番組での絞り込みを追加
 マイリスト作成どうするか？
 先生側で実行するときの手順書作る（デプロイしてたらいらないのかも）
 ＋ログボタンからは視聴日が未来でも入力できてしまう